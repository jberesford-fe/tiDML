% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dml-nnet.R
\name{dml_nnet}
\alias{dml_nnet}
\title{DML-PLR with single-hidden-layer MLP (nnet engine)}
\usage{
dml_nnet(
  data,
  y,
  d,
  x,
  folds_outer = NULL,
  v = 5,
  vcov_type = "HC2",
  hidden_units_m = NULL,
  hidden_units_g = NULL,
  penalty = 0.001,
  epochs = 200,
  max_weights = 5000,
  trace = FALSE
)
}
\arguments{
\item{data, y, d, x}{As in dml_rf()}

\item{folds_outer}{Optional rsample rset. If NULL, folds made internally (stratified on D).}

\item{v}{Number of outer folds when \code{folds_outer} is NULL.}

\item{vcov_type}{Sandwich variance type (e.g., "HC2" or "HC3").}

\item{hidden_units_m, hidden_units_g}{Hidden units for m- and g-models (NULL = heuristic).}

\item{penalty}{L2 penalty (a.k.a. weight decay).}

\item{epochs}{Max iterations (passed to \code{nnet::nnet()} via parsnip).}

\item{max_weights}{Max allowable weights for nnet engine (\code{MaxNWts}) to avoid overflow.}

\item{trace}{Logical; print nnet training trace.}
}
\description{
Uses a small MLP for both nuisances. Adds dummy encoding for nominal X
and normalizes numeric predictors (recommended for NNs).
}
